import 'package:flutter/material.dart';

class AboutFragment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new Text("About Fragment 2"),
    );
  }

}